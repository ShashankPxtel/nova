{
    "name": "nova-extension",
    "version": "1.0.0",
    "description": "Email phishing detection browser extension",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview --config vite.preview.js",
        "build:firefox": "npm run build && node scripts/prepare-firefox-build.mjs"
    },
    "devDependencies": {
        "@crxjs/vite-plugin": "^2.0.0-beta.28",
        "sharp": "^0.34.5",
        "vite": "^5.4.14"
    },
    "dependencies": {
        "firebase": "^12.11.0"
    }
}
